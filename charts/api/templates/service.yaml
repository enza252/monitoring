apiVersion: v1
kind: Service
metadata:
  name: {{ include "symbolapi.fullname" . }}
  labels:
    {{- include "symbolapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "symbolapi.selectorLabels" . | nindent 4 }}
